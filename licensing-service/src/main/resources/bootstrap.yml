spring:
  application:
    name: licensingservice
  profiles:
    active: dev
  cloud:
    config:
      enabled: true
      
management:
  endpoint:
    env:
      enabled: true
  endpoints:
    web:
      exposure:
        include: env